{
  "hooks": [
    {
      "event": "command:new",
      "action": "route_alert",
      "destination": "ops-channel",
      "description": "Notify operators when a new command is accepted"
    },
    {
      "event": "command:failed",
      "action": "route_alert",
      "destination": "ops-channel",
      "description": "Notify operators with failure context and run_id"
    }
  ]
}
