# WeeklyAI production audit — 2026-09-02

Goal: a useful, distinctive briefing for PMs discovering overlooked AI products, with truthful evidence, working API flows, and production deployment verification.

## Observed in production

- Frontend and backend at `c9a6859`; frontend https://weeklyai-six.vercel.app, backend https://backend-seven-ecru-62.vercel.app.
- MongoDB falls back to JSON because its configured Atlas SRV hostname does not resolve (Vercel runtime logs). This is not resolved by adding an IP alone. Atlas sign-in requested; no connection secrets copied into this report.
- Perplexity assistant returns 401 / quota exceeded. Current UI exposes the raw provider error and tries streaming then JSON.
- Homepage calls established businesses such as Databricks and Devin dark horses; stale historical entries silently backfill “this week.” Valuation and non-USD amounts affect funding ranking incorrectly.
- Conflicting English/Chinese monetary claims, source links absent on homepage, and repeated entries undermine trust. Existing source records need visible provenance and careful eligibility.
- Browser API defaults to localhost; SSR masks network/schema errors as empty results; requests have no timeout.
- GitHub CI verifies the legacy Express frontend, not the active Next app. Daily update suppresses discovery errors and does not sync MongoDB.
- Python baseline: 89 passed before stopping on 6 failures (hardware validation mismatch plus repository-cache pollution affecting later tests). Frontend baseline: 31 passed.

## Scope and design

Retain existing rose accent, light/dark themes, Chinese/English, discovery, news, search, and local favorites. Replace decorative hero noise and repeated boxed rankings with a product briefing, readable evidence, clear dates, and functional controls. No invented metrics or product facts.

## TCGPal alignment

Verified local main, fetched origin/main, and the production aliases `lenstcg.com` / `tcgpal.vercel.app` point to `9b71b708c1adf42211be621b9f2a58f1cb002a63`, deployment `dpl_Ez8ZDA2Sq28x5h21wQhWCMyL8S4r` (READY). A newer preview is separate. No application changes needed.

## Verification and remaining work

Implemented:

- Editorial briefing layout in existing rose palette; readable research rows, visible source links, discovery dates, archive labels and clear discovery-score meaning. Removed the decorative canvas and financing-led default order.
- Mobile News/Search navigation and research access, offscreen favorites visibility, dialog labels/focus handling, readable error messages, detail favorites/source links, UTC date display, and RSS HTML/entity cleanup.
- One same-origin Next API proxy for the browser, timeouts, status propagation and schema error visibility. Reduced homepage fetch from the entire catalog to 120 recent records; the full archive remains searchable. Unbounded archive requests bypass Next's 2 MB data-cache limit.
- Single-request research chat with six-turn history, query-relevant catalog retrieval, Chinese search terms, explicit archival framing, source links, sanitized provider failures, and no automatic duplicate retry/typewriter delay.
- Lazy Mongo connection, default database handling, connection cooldown and JSON fallback. Invalid DATA_PATH falls back to bundled data. Removed invented sample inventory. Health endpoint identifies active storage, configuration presence and actual product freshness.
- Preserved 1,152 existing production ID mappings; new IDs are deterministic and URL-safe across JSON/Mongo. Established products remain in the archive but are excluded from discovery; VC funds, incomplete explanations, unverifiable websites and untraceable sources are also excluded from recommendations.
- Provider preflight and empty-discovery failure in scheduled jobs; optional non-destructive Mongo upserts after snapshot publication. CI now tests the active Next app and Python code, rather than only legacy Express.
- Next.js upgraded from 16.1.6 to 16.3.4; production dependency audit went from four high-severity findings to zero.
- Vercel builds use Node.js 24 and omit Docker-only standalone output when Vercel's Next adapter is active, avoiding the Next 16.3 adapter/trace incompatibility. Translation gaps and short descriptions remain visible data-quality warnings without making every release red; malformed or missing core fields still fail verification.

Source corrections:

- Latent Labs: historical February 2025 funding, $50M total including $40M Series A, London/San Francisco context, linked to [the company announcement](https://www.latentlabs.com/press-release/latent-labs-secures-50m-in-funding/).
- 1X: removed Japan attribution; recorded US/Norway dual-headquarters history with [the company's 2024 announcement](https://1x-tech.webflow.io/discover/1x-opening-new-head-quarters-in-sunnyvale-ca).
- ModelBest: corrected English RMB 10B to RMB 1B, supported by [the original report](https://36kr.jp/492998/).
- DinoTicia: removed conflicting USD conversion claims and retained needs-verification status.
- Humane AI Pin 2: removed an unsupported product record. Its cited 2023 article described the first-generation Pin, while Humane discontinued the hardware business and services in February 2025; there is no official second-generation product.

Verification completed locally: 147 Python tests plus 11 subtests; 35 Vitest tests; TypeScript and ESLint; Next.js 16.3.4 production build; npm production audit with zero findings. Browser-checked Chinese/English, light/dark and phone layouts, source/details, search returning Cradle, favorites saving/removal controls, discovery skip/save, news, and missing-key chat via the proxy. Screenshots and test outputs are in `artifacts/audit-2026-09-02/`.

Remaining external requirements:

1. Atlas is signed out in both available browsers. Repair the missing cluster connection string and add the user's current IP after authenticated access is available. The local computer's IP does not authorize Vercel's separate outbound network.
2. The currently configured Perplexity key returns quota exceeded. Real successful generation and a fresh discovery run need usable credits/key access. No credits purchased and no keys exposed.
3. The catalog is historical and automatically summarized. Eligibility checks and spot corrections do not constitute a factual review of every archived claim. Fresh source-backed discovery remains a release acceptance condition once provider access is restored.

Deployment verification will be appended after the release. Configuration instructions are in `DEPLOYMENT.md`.
